{
  "name": "posettakip",
  "private": true,
  "homepage": "https://muhammedko90.github.io/posettakip/",
  "scripts": {
    "postinstall": "patch-package && node scripts/ensure-capacitor-vendor.cjs && node scripts/ensure-local-firebase-files.cjs",
    "dev": "serve . -l 5173",
    "start": "serve . -l 5173",
    "copy:www": "node scripts/copy-www.cjs",
    "cap:sync": "npm run copy:www && npx cap sync android",
    "android:sync": "npm run cap:sync",
    "cap:open": "npx cap open android",
    "android:build": "npm run cap:sync && cd android && .\\gradlew.bat assembleDebug",
    "android:release": "npm run cap:sync && cd android && .\\gradlew.bat assembleRelease",
    "firebase:deploy-rules": "firebase deploy --only firestore:rules,storage --project emre-bebe-takip",
    "functions:install": "npm --prefix functions install",
    "functions:deploy": "firebase deploy --only functions:fcm --project emre-bebe-takip",
    "functions:logs": "firebase functions:log --project emre-bebe-takip",
    "functions:emulate": "firebase emulators:start --only functions,firestore --project emre-bebe-takip"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/share": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "firebase-tools": "^13.0.0",
    "patch-package": "^8.0.1",
    "serve": "^14.2.4"
  }
}
